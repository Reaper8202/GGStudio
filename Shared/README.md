# Shared

Cross-game assets (art, audio, fonts, etc.) used by more than one project in this repo. Tracked via Git LFS — see `.gitattributes` at the repo root.

Games reference or copy assets from here rather than duplicating them.
