You are Codex Agent H on Scrap Rig (3D vehicle builder, Three.js + DOM UI, strict TS). Implement the EDITOR half of the approved simplification spec.

READ FIRST: docs/agent-reports/kids-simplification-review.md (sections B, C, and the C bug analysis), src/core/types.ts (PAINT_COLORS/PaintColor/PartConfig.paint exist), src/editor/EditorMode.ts, src/editor/ui.ts, src/editor/meshes.ts, src/editor/TutorialOverlay.ts, src/app/App.ts, src/style.css, tests/seam.ts.

CONCURRENT AGENT: another agent is simultaneously rewriting src/core/parts.ts (catalog shrinks to: chassis-core [pre-seeded root, NOT in palette], frame-box, frame-reinforced, wheel-standard, wheel-offroad, driver-seat, engine-small, fuel-tank, turret — all attach via ordinary 'frame' sockets, no mounts), src/core/tutorial.ts (SIMPLE_PART_IDS = those 8 palette ids; 6 steps), src/core/placement.ts, src/core/serialize.ts, src/runtime/damage.ts. Code against that as ground truth. Do NOT touch any file that agent owns, or src/core/* or src/runtime/* at all.

ORCHESTRATOR AMENDMENTS to the spec (override it):
- No "kids"/"simple" wording in code or UI copy; there is exactly ONE mode.
- No dropdown menus: top bar keeps the inline name input + Save + Load(select) + New; REMOVE the Duplicate button. Replace the five view buttons with one native <select> (3D / Front / Rear / Side / Top; keys 1–5 still work).
- Selected-part toolbar: FIXED position (bottom-centre, above the bottombar, like the tutorial panel but smaller and offset when tutorial visible) — do not project beside the 3D part.
- Skip the 'Move' tool for now (out of scope): toolbar = Turn (yaw) · Flip (pitch) · 6 paint swatches · Delete. Everything else in spec C applies.

YOU OWN EXCLUSIVELY: src/editor/EditorMode.ts, src/editor/ui.ts, src/editor/meshes.ts, src/editor/TutorialOverlay.ts (only if needed), src/app/App.ts, src/style.css, tests/*.ts (all Playwright specs + seam.ts).

TASK 1 — Picking fixes (spec C1/C2, P0):
- In buildPartMesh (meshes.ts): set `userData.placementSurface = true` on the solid cube/cylinder/slab Meshes (not edges/notches).
- EditorMode: set `this.raycaster.params.Line.threshold = 0`; in updateGhost and selectAt pick the nearest intersection whose object is a visible Mesh with a non-null face and placementSurface (walk hits, not hits[0]).

TASK 2 — Editing feel (spec C4/C6/C7):
- One-shot placement: after a successful place, disarm the ghost and select the newly placed part (showing its toolbar). No repeat-stamping.
- Right-click (stationary, <6px movement) ALWAYS deletes the part under the cursor (armed or not), except the root: show status/toast "🔒 Truck Heart can't be deleted". Suppress the browser context menu on the canvas. Keep Del/Backspace on selection and Esc.
- Palette gains a red "🧽 Erase" tile pinned under the parts: arms erase mode — left-click deletes the part under cursor (root protected); Esc or clicking Erase again exits. While ANY tool is armed show a "× Cancel" chip near the palette bottom.
- Revalidate placement at commit time: on pointer-up re-run canPlacePart with the CURRENT blueprint and ghost orientation before executing; after R/F rotation immediately re-run updateGhost with the last cursor position (store it).
- Reuse one ghost mesh per armed defId+orient (rebuild only when defId/orient/validity-colour changes; always update position) to stop per-mousemove rebuild churn.

TASK 3 — UI simplification (spec B + amendments):
- Palette: single mode, no headers, no mass/cost — the 8 SIMPLE_PART_IDS tiles (import from core/tutorial.ts) with KID_LABELS name+blurb, then Erase. Delete the palette-mode toggle and its localStorage key and the 'all' rendering path entirely.
- Bottom bar: keep ONLY the layer slider + status text. Delete X-ray/Structure/Hide armour/Hide shell/CoM/Contacts/Support/Links/Arcs buttons and onViewMode/onOverlayToggle handlers; overlays default: com=true, contacts=true, supportPolygon=true, connections=false, arcs=false (fixed, no UI).
- Layer slicing: parts fully above the active layer must be excluded from picking (skip them in updateGhost/selectAt hit filtering — check mesh visibility/opacity flag you set at rebuild), not just faded.
- Right side: DELETE the inspector/stats/issues stack. New compact build card (top-right, one panel): "Weight: N kg", "Stability: 😀 Great | 🙂 Okay | 😟 Tippy | 🛑 Will tip" (map rolloverRisk low/medium/high/extreme), then at most TWO lines: hard errors first (kid-friendly text from the ValidationIssue messages), else first warning, else "✅ Ready to drive". Keep TEST DRIVE gating exactly as now.
- Wheel auto-config ALWAYS (single mode): after EVERY blueprint change (centralize in refresh()), normalize all wheels: driven=true, braking=true, steerInverted=false, suspensionPreset='standard', steering = wheel z > (minWheelZ+maxWheelZ)/2 (all steer when a single axle). Implement as a silent blueprint fixup (not an undoable command): map over parts before analysis/render. Remove the wheel checkboxes/preset dropdown from the selected-part UI.
- Selected-part toolbar (replaces inspector selection UI): part name + Turn/Flip buttons (reuse rotateSelected), 6 paint swatches (PAINT_COLORS; apply via updateConfigCommand {...config, paint}), Delete. Root part: only paint swatches (no delete). meshes.ts: when placed.config.paint is set, use PAINT_COLORS[paint] instead of the default part colour for the main body meshes (wheels keep dark tires; paint the hub).
- Top bar final contents: name input, slots select, Save, Load, New, Undo, Redo, Symmetry toggle ("Build both sides"), View <select>, 🎓 Tutorial, ? Help, ▶ TEST DRIVE. New: replaceBlueprint(createTutorialBlueprint()-style seeded bp) — i.e. New must seed the locked Truck Heart at (0,1,0) (use createEmptyBlueprint + add chassis-core part 'p1'; do not import from tutorial.ts for this if awkward).
- Update the Help overlay text to match the new reality (no mounts: "wheels snap onto the sides of blocks, engines on top", right-click erases, paint swatches). Keep the welcome dialog + tutorial flow, copy without "zombie truck?" changes needed except neutral tone.

TASK 4 — App.ts: starter blueprint: replace the four wheel-mount parts and the engine-mount with frame-box at the same coordinates (wheels/engine stay where they are); remove suspensionPreset/steering flags from starter configs (normalization now handles it). Keep debug seam as-is (debugPlace unchanged; it bypasses normalization only until next refresh — acceptable).

TASK 5 — Playwright tests (WRITE ONLY, do not run): update tests/seam.ts buildBasicRig (mounts → frame-box, remove config args — rely on auto-config; keep yaw180 for right wheels), tests/editor.spec.ts (armour test → replace with: stacking regression [place frame-box at (0,2,0) ON TOP of chassis-core via seam AND assert ok; note seam bypasses picking, so ALSO add a UI-level stacking test: arm Block via palette click, dispatch pointer events over the canvas centre where the starter deck is, assert parts count grew — if too flaky, instead add a pure regression: raycaster picking covered implicitly and keep seam test], right-click delete test: select nothing, dispatch contextmenu/right pointerdown+up over a placed part, assert part count decreased), tests/tutorial.spec.ts (6 steps, no mounts stage), tests/combat.spec.ts (turret is self-contained: no ammo-box needed; REPLACE the drop-breaks-parts assertion with the new acceptance: drop scenario with correct car loses NOTHING (aliveParts unchanged, detached 0); add ramp full-throttle 6s → zero loss; keep weapons-fire ammo test), tests/drive.spec.ts + failure.spec.ts (update rig builders; heavy-ramp test: reinforced slab still fine; sideways wheel test: wheels now mount via frame sockets — rollX90 orientation still places against a frame-box side, verify logic unchanged), NEW assertions where cheap.

RULES: `npm run typecheck` and `npx eslint .` must pass at the END (the concurrent agent's catalog may land mid-run; re-run typecheck at the end; if failures are ONLY inside unit/ or src/core//src/runtime (other agent's files), report them in Issues instead of fixing). Do NOT run playwright or vitest conclusions as gate (maintainer runs them). No new dependencies. Report: Done / Files changed / Tests run / Assumptions / Issues / Next recommended task.
